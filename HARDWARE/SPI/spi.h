#ifndef __SPI_H
#define __SPI_H
#include "sys.h"
//////////////////////////////////////////////////////////////////////////////////				////////////////////////////////////////////////////////////////////////////////// 	

u8 SPI_Init(void);												//初始化SPI
u8 SPI_Receive(u8* buf,u32 datalen);							//SPI接收数据
u8 SPI_Transmit(u8* buf,u32 datalen);							//SPI发送数据
#endif
